# Mastering the preview

The right panel shows the continuously compiled document. A few useful settings:

- ^zoom_in^ / ^zoom_out^: zoom in or out the preview;
- zoom field: type a percentage directly;
- ^refresh^ **Compile**: force a manual recompilation (useful for heavy documents).

Click any element in the preview: the editor cursor jumps to the corresponding position in the source code.