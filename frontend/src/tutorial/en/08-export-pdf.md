# Exporting to PDF

The ^picture_as_pdf^ **Save** toolbar button exports your compiled document to PDF.

- On the first export, pick the file location.
- Subsequent exports reuse the same location (until the app is closed).

The produced PDF includes the real rendering: images, tables, bibliographies and full layout.